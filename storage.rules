rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // DEVELOPMENT ONLY: Allow all read/write access
      allow read, write: if true;
    }
  }
}
